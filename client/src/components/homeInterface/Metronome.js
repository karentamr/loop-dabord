import React, { useContext, useEffect, useRef } from "react";
import { LooperContext } from "../context/LooperContextProvider";

const Metronome = () => {
  const { ctx, masterPositionBeat,isPlaying } = useContext(LooperContext);

  

  return null;
};

export default Metronome;